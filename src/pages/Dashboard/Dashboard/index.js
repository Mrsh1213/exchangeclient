import React from 'react';
import PropTypes from 'prop-types';

Index.propTypes = {};

function Index(props) {
    return (
        <div style={{color: "#fff"}}>Dashboard Not building</div>
    );
}

export default Index;